import extism # noqa: F401 # pyright: ignore
from pdk_types import <%- Object.values(schema.schemas).map(schema => schema.name).join(", ") %> # noqa: F401

<% schema.exports.forEach(ex => { -%>
def <%- camelToSnakeCase(ex.name) %>(<% if (ex.input) { %>input: <% if (ex.input.contentType === "application/json") { %> <%- toPythonType(ex.input) %> <% } else { %> <%- toPythonType(ex.input) %> <% } %> <% } %>) <% if (ex.output) {%>-> <%- toPythonType(ex.output) %><%}%>:
  pass
<% }) %>

