# THIS FILE WAS GENERATED BY `xtp-python-bindgen`. DO NOT EDIT.

import extism # pyright: ignore
import plugin
import pdk_imports as imports # noqa: F401
from pdk_types import <%- Object.values(schema.schemas).map(schema => schema.name).join(", ") %> # noqa: F401 

<% schema.exports.forEach(ex => { -%>
@extism.plugin_fn
def <%- ex.name %>():
  <% if (ex.output && ex.output.contentType === "application/json") { %>
  res = plugin.<%- camelToSnakeCase(ex.name) %>(<% if (ex.input) { %>  <% if (ex.input.contentType === "application/json") { %> extism.input(<%- toPythonType(ex.input) %>) <% } else { %> extism.input(<%- toPythonType(ex.input) %>) <% } %> <% } %>)
  <% } else { %>
  res = plugin.<%- camelToSnakeCase(ex.name) %>(<% if (ex.input) { %> <% if (ex.input.contentType === "application/json") { %> extism.input(<%- toPythonType(ex.input) %>) <% } else { %> extism.input(<%- toPythonType(ex.input) %>) <% } %> <% } %>)
  <% } %>
  extism.output(res)

<% }) %>
